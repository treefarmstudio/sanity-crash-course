---
import Layout from '../layouts/Page.astro';

import { getFirstBlogPost, getSiteSettings } from '../lib/api';

const settings = await getSiteSettings();

// const firstPost = await getFirstBlogPost();
---

<Layout>
	<main>
		<h1>Welcome to <span class="text-gradient">Sanity + Astro Crash Course</span></h1>
		<p>
      {JSON.stringify(settings)}
    </p>
	</main>
</Layout>
